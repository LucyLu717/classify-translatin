# REU Application Task
## Quick start

```
pip install -r requirements.txt
python -W ignore model.py logreg bleu 
```

Documentation for `model.py`.

```
usage: model.py [-h] model [model ...]

positional arguments:
  model       name of the model: logreg bleu
```